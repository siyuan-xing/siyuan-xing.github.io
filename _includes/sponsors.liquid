<div class="sponsors-logos">
  {% for sponsor in site.data.sponsors %}
    <div class="sponsor-logo sponsor-logo--{{ sponsor.class }}">
      <img src="{{ sponsor.logo | relative_url }}" alt="{{ sponsor.name }} logo" loading="lazy">
    </div>
  {% endfor %}
</div>
